defaults:
  - optimal_sample
  - _self_

# Interpreted as ceil(multiplier * batch_pair_budget / n_annotators).
# Study grids use 1.0, 2.0, and 4.0 for quota-like, moderate, and loose caps.
max_per_annotator_mode: relative_batch_share
max_per_annotator_multiplier: 2.0
on_infeasible: relax
